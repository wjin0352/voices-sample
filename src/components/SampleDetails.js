import React, { Component } from 'react';
import { connect } from 'react-redux';

class SampleDetails extends Component {
  render() {

    return (
      <div></div>
    );
  }
}

export default connect(null)(SampleDetails);
